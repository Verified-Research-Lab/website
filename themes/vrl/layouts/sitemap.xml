{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML -}}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{- range site.Pages -}}
    {{- if and (ne .Kind "404") (not .Params.redirectTarget) (or .IsHome .IsSection .IsPage) }}
  <url><loc>{{ .Permalink }}</loc>{{ with .Lastmod }}{{ if not .IsZero }}<lastmod>{{ .Format "2006-01-02T15:04:05Z07:00" }}</lastmod>{{ end }}{{ end }}</url>
    {{- end -}}
  {{- end }}
</urlset>
